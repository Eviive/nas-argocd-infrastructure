{{ $name := include "base.component-fullname" (dict "context" $ "component" "default") }}
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: {{ $name }}
spec:
  ipAddressPools:
    - {{ $name }}
