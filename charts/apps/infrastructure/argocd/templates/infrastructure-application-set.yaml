apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: infrastructure
  labels:
    {{- include "base.labels" (dict "context" $ "component" "infrastructure") | nindent 4 }}
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - git:
        repoURL: https://github.com/Eviive/nas-argocd-infrastructure
        revision: HEAD
        files:
          - path: charts/apps/infrastructure/*/config.json
  template:
    metadata:
      name: "{{ `{{ .name }}` }}"
    spec:
      project: infrastructure
      source:
        repoURL: https://github.com/Eviive/nas-argocd-infrastructure
        path: "charts/apps/infrastructure/{{ `{{ .name }}` }}"
        helm:
          valueFiles:
            - values.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ `{{ .name }}` }}"
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
          - "ServerSideApply={{ `{{ default \"false\" (get . \"serverSideApply\") }}` }}"
