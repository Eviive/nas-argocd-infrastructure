apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: apps
  labels:
    {{- include "base.labels" (dict "context" $ "component" "apps") | nindent 4 }}
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - git:
        repoURL: https://github.com/Eviive/nas-argocd-infrastructure
        revision: HEAD
        files:
          - path: charts/apps/apps/*/config.json
  template:
    metadata:
      name: "{{ `{{ .path.basename }}` }}"
    spec:
      project: apps
      source:
        repoURL: https://github.com/Eviive/nas-argocd-infrastructure
        path: "charts/apps/apps/{{ `{{ .name }}` }}"
        helm:
          valueFiles:
            - values.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ `{{ .name }}` }}"
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
          - "ServerSideApply={{ `{{ default \"false\" (get . \"serverSideApply\") }}` }}"
